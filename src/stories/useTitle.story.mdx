import { Meta } from '@storybook/addon-docs'
import useTitle from '../hooks/useTitle'
import Title from './useTitle.demo.tsx'

<Meta title='hooks/useTitle' />

<h2 style={{ color: '#ff3399' }}> UseTitle Demo </h2>

<Title />

<h2 style={{ color: '#ff3399' }}>UseTitle Docs </h2>
简单的处理下标题

```js
import useTitle from '../hooks/useTitle'

export default function Title() {
  return (
    <button
      onClick={() => {
        useTitle('changed')
      }}
    >
      change webSite title
    </button>
  )
}
```
