import { Meta } from '@storybook/addon-docs'
import Title from './useTitle.demo.tsx'
import Home from './Home'

<Meta title='hooks/useTitle' />

<h2 style={{ color: '#ff3399' }}> UseTitle Reference </h2>

<Title />
<Home />

<h2 style={{ color: '#ff3399' }}>UseTitle Usage </h2>
简单的处理下标题

```js
import { useTitle } from 'wrench-hooks'

export default function Title() {
  return (
    <button
      onClick={() => {
        useTitle('changed')
      }}
    >
      change webSite title
    </button>
  )
}
```
