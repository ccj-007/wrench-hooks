import { Meta } from '@storybook/addon-docs'

<Meta title='Introduction' />

<style>
  {`
    .title {
      color: #ff3399;
    }
    .use {
      width: 200px;
      text-align: center;
      background: #ff3399;
      color: #fff;
    }
    .use-box {
      display: flex;
      margin-bottom: 20px;
    }
    .desc {
      width: 400px;
      text-align: center;
      background: #eee;
      color: #000;
    }
  `}
</style>

<h1 className='title'>Welcome to wrench-hooks</h1>

wrench-hooks 扳手 hooks 工具库，针对<span className='title'>业务场景</span>开发的 hooks，轻量简洁~~~

```js
//Quick Start
npm i wrench-hooks

import { useCallbackState } from 'wrench-hooks'
```

export const UseText = () => {
  const mapList = [
    {
      usename: 'useCallbackState',
      text: '用于获取最新的 state',
    },
    {
      usename: 'useDebounce',
      text: '常见用于input的输入的防抖处理',
    },
    {
      usename: 'useThrottle',
      text: '按钮点击必用的节流处理',
    },
    {
      usename: 'useMouce',
      text: '跟踪鼠标的位置',
    },
    {
      usename: 'useScroll',
      text: '跟踪网页滚轮的位置',
    },
    {
      usename: 'useWindowSize',
      text: '跟踪网页的尺寸缩放',
    },
    {
      usename: 'useFullScreen',
      text: '全局显示元素或视频',
    },
    {
      usename: 'useClip',
      text: '文本复制到剪贴板',
    },
    {
      usename: 'useTitle',
      text: '修改标题',
    },
  ]
  return mapList.map((item, index) => {
    return (
      <div className='use-box' key={index}>
        <div className='use'>{item.usename}</div> <div className='desc'>{item.text}</div>
      </div>
    )
  })
}

<UseText> </UseText>
