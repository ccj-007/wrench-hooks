import { Meta } from '@storybook/addon-docs'
import Update from './useUpdate.demo.tsx'
import Home from '../Home'
import { HooksText } from '../introduction'

<Meta title='hooks/useUpdate' />

<h2 style={{ color: '#ff3399' }}> useUpdate Reference </h2>

<Update />
<Home />

<h2 style={{ color: '#ff3399' }}>useUpdate Usage </h2>

<HooksText hookname={'useUpdate'} />

```js
import { useUpdate } from 'wrench-hooks'

const Update = () => {
  const update = useUpdate()

  return (
    <>
      <div>Time: {Date.now()}</div>
      <button onClick={update}>Update</button>
    </>
  )
}

export default Update
```
