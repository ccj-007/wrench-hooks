import { Meta } from '@storybook/addon-docs'
import Memo from './createMemo.demo.tsx'
import Home from '../Home'
import { HooksText } from '../introduction'

<Meta title='hooks/createMemo' />

<h2 style={{ color: '#ff3399' }}> createMemo Reference </h2>

<Memo />
<Home />

<h2 style={{ color: '#ff3399' }}>createMemo Usage </h2>

<HooksText hookname={'createMemo'} />

```js
import { createMemo } from 'wrench-hooks'

const Memo = () => {
  const fibonacci = (n: number): number => {
    if (n === 0) return 0
    if (n === 1) return 1
    return fibonacci(n - 1) + fibonacci(n - 2)
  }

  const useMemoFibonacci = createMemo(fibonacci)

  const result = useMemoFibonacci(10)

  return <div>fib(10) = {result}</div>
}

export default Memo
```
